#include "service/events/btn.h"

void initEvents() {
  initButtonEvents();
  // initTouchEvents();
  // initVolumeEvents();
  // initPowerEvents();
  // initBootEvents();
  // initMicEvents();
  // initSpeakerEvents();
  // initBatteryEvents();
  // initChargerEvents();
  // initSDCardEvents();
  // initUSBEvents();
  // initWiFiEvents();
  // initBluetoothEvents();
  // initNFCEvents();
  // initGPSModuleEvents();
}